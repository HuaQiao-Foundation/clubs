# Cloudflare Pages Environment Variables
# Copy this to Cloudflare Pages dashboard > Settings > Environment variables

# Supabase Configuration (Required)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Environment Configuration
VITE_APP_ENV=production

# Feature Flags (Optional)
VITE_CHINA_MODE=true
VITE_ENABLE_ANALYTICS=false

# API Configuration (if needed)
VITE_API_TIMEOUT=30000

# Note: All environment variables must start with VITE_ to be accessible in the app
# Set these in Cloudflare Pages dashboard, not in a .env file for production